# Copy to .env.local (git-ignored). The key is only read by the dev proxy /
# Vercel function — it is never shipped to the browser.
GEMINI_API_KEY=
# Optional: override the model (default: gemini-flash-latest, free tier)
GEMINI_MODEL=
